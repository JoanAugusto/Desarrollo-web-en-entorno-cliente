'use strict '

function ucFirst(str){
    let getFirstChar=str[0];//Recordar que un String es como un array podemos acceder mediante por posicion.
    ;
}
ucFirst("doggy");let parser = getFirstChar.toUpperCase() + str.slice (1);

    let getFirstChar2=str[0].toUpperCase()+str.slice(1);// mas rapido sin en un sola linea de codigo.s
    
    console.log(parser);
    console.log(getFirstChar2)